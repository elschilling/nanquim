# Nanquim v0.1.0-alpha.2

Release date: **2026-09-20**. [Immutable release](https://github.com/elschilling/nanquim/releases/tag/v0.1.0-alpha.2).

`v0.1.0-alpha.2` brings safer native SVG documents, more predictable editing,
an original visual identity, image editing, and expanded CAD tools. It remains
an alpha for evaluation and compatibility feedback. Keep independent copies of
important drawings, especially before opening files saved by an older version.

## Highlights

- New/Open/Save/Save As share unsaved-change protection and a transactional
  document lifecycle. Rejected files leave the current drawing intact, and
  native SVG saves preserve collections, styles, blocks, Paper data, imported
  definitions, and Geometry Nodes metadata through the qualified round trips.
- Nanquim now has its own pen-nib mark and command icons, three appearance
  presets, custom accent/background colors, and a resizable command palette.
  F1 opens searchable illustrated Help; F4 toggles the palette.
- Local PNG, JPEG, GIF, and WebP images can be inserted with `IMAGE` or file
  drop, resized, moved, and cropped with Undo/Redo. Images are embedded, and
  cropping remains editable after native SVG save/reopen and clipboard copy.
- Outliner drag-and-drop, keyboard moves, Shift-click ranges, and an `M`
  collection-move dialog make drawing organization easier while preserving
  positions, styles, and Undo/Redo.
- `JOIN` combines supported connected lines and curves; `OFFSET` adds open or
  closed polylines and circular arcs; `FILLET` rounds rectangle corners; and
  `HATCH` accepts selected rectangles and explicitly closed paths, including
  curved and compound boundaries. These operations reject unsupported input
  before changing the drawing.
- Dimension styles add orientation and text-position controls. Dimensions and
  distance measurements have live guides; snapping, polyline closure, spline
  Ortho, Mirror, Trim, and Extend receive focused workflow fixes.
- Completed editing operations use consistent History transactions for the
  qualified paths. Rotate preserves affine-transformed geometry and metadata;
  command cancellation, grip edits, and Undo/Redo have broader regression
  coverage.
- One-sheet Paper output and SVG/DXF exchange have explicit qualification
  profiles, diagnostics, browser workflows, and reproducible performance
  budgets. Click the Nanquim icon to reopen Welcome and browse dated changes.

See the [changelog](https://github.com/elschilling/nanquim/blob/v0.1.0-alpha.2/CHANGELOG.md)
for individual changes and their implementation commits.

## Compatibility

- Application version: `0.1.0-alpha.2`.
- Native Nanquim SVG saves use document schema **3**. This release includes the
  schema-v3 document work added after `v0.1.0-alpha.1`, which saved schema 2.
  Supported schema-1 and schema-2 documents migrate in memory on Open and stay
  marked unsaved until an explicit Save writes the current format. Use Save As
  and keep the original when compatibility with an older Nanquim matters.
- Geometry Nodes document metadata and graph schemas remain **1**.
  This application-version update does not advance either saved-data schema.
- Desktop Chromium stable and the previous stable release are the primary
  browser targets. Firefox stable and ESR use portable upload/download file
  workflows. Safari/WebKit remains best-effort; mobile and touch-first editing
  are not supported.
- Direct overwrite and persistent recent-file handles require a secure context
  and the browser File System Access API. Download-only Save As keeps the
  drawing marked unsaved because the browser cannot confirm the file was kept.
- The document source limit is 64 MiB, with a 100,000-element SVG safety budget.
  Images are Model Space only, one file at a time, up to 8 MiB and 40 million
  decoded pixels per image. These are safety limits, not performance promises.

See the [native document contract](https://github.com/elschilling/nanquim/blob/v0.1.0-alpha.2/docs/native-document-format.md)
and [browser support policy](https://github.com/elschilling/nanquim/blob/v0.1.0-alpha.2/docs/browser-support.md).

## Known limitations

- SVG/DXF exchange and Paper SVG/PDF output remain **Partial**. Native SVG is
  the editable source; presentation and interchange exports can lose features.
  Inspect exported files in the intended receiving application.
- DXF exchange covers common Model entities and representable layers, not every
  entity or vendor extension. DXF text import, native metadata, Paper data, and
  arbitrary SVG paint or curves are outside the qualified profile.
- Paper supports one sheet with multiple viewports. Per-color plot lineweights,
  multiple sheets, and title-block workflows remain deferred. Color mapping
  preserves existing stroke widths.
- Many modification tools deliberately reject transformed or specialized
  geometry outside their qualified cases. JOIN requires connected,
  non-branching geometry sharing a parent; mixed joined curves retain their
  SVG shape but cannot retain each source's specialized arc/spline grips.
- Inspector fields, collection settings, and saved-view navigation mark the
  drawing dirty but are not individual Undo entries. New/Open reset History.
- Cropping hides pixels without deleting them: the original image remains
  embedded in the drawing and clipboard copies. Raster tracing, external image
  links, and raster DXF/PDF interchange are outside the qualified workflow.
- Geometry Nodes remains **Experimental**. Source elements need a shared parent
  when attaching a modifier; direct Blender/Bonsai connectivity is not shipped.
- Browser clipboard permissions, persistent handles, and external-renderer
  equivalence require the candidate checks recorded below. System-only fonts
  are not portable; Fira Code Italic may appear synthetic in the browser and
  upright in PDF. Prefer qualified bundled font variants for consistent output.

The [capability ledger](https://github.com/elschilling/nanquim/blob/v0.1.0-alpha.2/docs/capabilities.md)
and [interoperability guide](https://github.com/elschilling/nanquim/blob/v0.1.0-alpha.2/docs/file-interoperability.md)
describe the supported boundaries. This release does not promote Partial or
Experimental areas to Stable.

## Qualification record

Qualification ran on 2026-09-20. The release candidate merged through
[PR #32](https://github.com/elschilling/nanquim/pull/32) is
[`37f9e085f81b19f92581d9cf85bd96ad6f131e8e`](https://github.com/elschilling/nanquim/commit/37f9e085f81b19f92581d9cf85bd96ad6f131e8e).
The records below identify the source used for each check. The
[release tag](https://github.com/elschilling/nanquim/tree/v0.1.0-alpha.2)
identifies the final metadata commit; its exact SHA, CI, and deployment record
are appended to the published GitHub release after final verification.

| Check | Result |
| --- | --- |
| Environment | Node.js 22.22.2, pnpm 8.15.5, Fedora Linux 44 Workstation, Linux 7.2.5-200.fc44.x86_64. |
| Frozen dependency installation | `pnpm install --frozen-lockfile` passed; pnpm lockfile format 6 does not store the root application version and required no changes. |
| Vitest | 1,259 tests passed across 97 files. |
| V8 coverage | Statements 67.93%, branches 60.07%, functions 75.74%, lines 70.59%; configured coverage gates passed. |
| Production build | Passed. The existing main-chunk warning above 500 kB remains; this release does not claim to resolve that size debt. |
| Local Chromium workflows | Chrome 153.0.8010.36 passed all 43 steps; `test-results/browser/chromium/summary.json`. |
| Local Firefox workflows | Firefox 156.0 passed all 43 steps with the corrected crop-preview wait; `test-results/browser/firefox/summary.json`. |
| External SVG profile | Exact source `1a6665d4ba3183700f778944090ac6b92d2fe3e5`: Inkscape 1.4.4 retained physical size, inventory and references; all 21 bounds matched with maximum delta 0. Blender 5.2.1 LTS imported 17 objects, 14 curves and 14 splines. `test-results/interoperability/alpha2-external-1a6665d4/summary.json`. |
| Performance | All 20 metric/dataset checks passed their median and p95 budgets after one warm-up and five samples; details below. |
| Dependency audit | High-severity gate passed with zero high/critical findings. One moderate advisory remains in transitive `fflate` 0.8.2: [GHSA-px8p-9vwx-vf98](https://github.com/advisories/GHSA-px8p-9vwx-vf98), malformed ZIP64 handling in `unzipSync`. |
| Release PR CI | Passed [run 35544004587](https://github.com/elschilling/nanquim/actions/runs/35544004587); merged-candidate result is recorded separately below. |
| Merged-candidate CI | Passed [run 35544137326](https://github.com/elschilling/nanquim/actions/runs/35544137326) at `37f9e085f81b19f92581d9cf85bd96ad6f131e8e`. |
| Remote browser matrix | [Run 35543929819](https://github.com/elschilling/nanquim/actions/runs/35543929819) passed at `349fb608e4b8f081ba6a8d1ed0010576b2d54836`: Chrome 153.0.8010.52 and 152.0.7977.82, Firefox 156.0 and ESR 140.16.0, all 43 steps each on Ubuntu 24.04 (runner image `20260907.300.1`). |

The initial stable-Firefox matrix attempt caught a harness timing race: the
crop check could observe a small grip-click preview before the destination
pointer frame. Commit
[`349fb60`](https://github.com/elschilling/nanquim/commit/349fb608e4b8f081ba6a8d1ed0010576b2d54836)
waits for the intended crop inset at the existing tolerance. The successful
matrix includes that fix; no product assertion or tolerance was weakened.

Performance used Chrome 153.0.8010.36 at 1440 × 900 on the environment above,
an AMD Ryzen 7 1800X with 8 logical CPUs and 33,587,441,664 bytes of RAM.
The 1,000-element checksum is `fnv1a-e81dcdcc`; the 10,000-element checksum is
`fnv1a-f55245f4`. Full samples and budgets are in
`test-results/performance/chromium/summary.json`. These results qualify this
dataset and environment, not arbitrary drawing sizes or a speed improvement.

### Manual exchange and remaining gates

A LibreCAD 2.2.1.2 GUI round trip on the `1a6665d4` source exported, opened,
edited, saved as ASCII R2000, and reopened a five-entity sample in Nanquim.
Centimetre units, four named layers, hidden/locked states, geometry counts,
rectangle/polyline closure, bounds, and the intended line endpoint edit were
preserved. LibreCAD rewrote the Hidden layer ACI color from `-7` to `256`
while retaining its frozen flag. This external color degradation is recorded;
the sample does not establish a complete layer-color round-trip guarantee.
Evidence: `test-results/interoperability/alpha2-paper/external-review.json`
and `test-results/interoperability/alpha2-paper/librecad-reopen-browser.json`.

The same external review inspected a 500.5 × 321.25 mm Paper sheet with two
viewports using Inkscape 1.4.4 and Poppler 26.01.0. Both renders retained the
viewport line pairs, clipping, color mapping, existing widths, and Paper
annotation. PDF inspection found one page, no raster images, embedded Inter
with Unicode mapping, and extractable measurement text. The synthetic fixture
inherited a thick stroke onto its text, making the Inkscape text illegible;
actual dimension commands explicitly set `stroke: none`. This observation
does not establish a dimension-command regression, and the broader external
visual-equivalence/readable-dimension gate remains open. No printer output
was inspected.

Direct persistent-handle overwrite/reopen and Recent Files in Chromium, and
best-effort Safari/WebKit on macOS, remain unrecorded. Paper and exchange
capabilities stay Partial, Geometry Nodes stays Experimental, and Phase 3
remains In progress.

## Production verification

Vercel successfully deployed candidate `37f9e085f81b19f92581d9cf85bd96ad6f131e8e`
at `nanquim-jb333clek-elschillings-projects.vercel.app`; the public
[nanquim.vercel.app](https://nanquim.vercel.app/) alias serves alpha.2.
On Fedora 44, Chrome 153.0.8010.36 and Firefox 156.0 passed startup/version
checks at 1280 × 800 and 960 × 800, with no console, page, or HTTP errors and
no horizontal overflow. Normal and narrow screenshots were inspected.
Evidence: `test-results/browser/alpha2-production-smoke/summary.json`.

Both browsers passed all 43 production workflows against this candidate with
zero page, request, or HTTP errors. Coverage includes drawing/selection and
Undo/Redo, native SVG save/reopen, Paper SVG/PDF output, DXF diagnostics,
raster editing, Outliner, and Welcome history. Firefox used an immutable
archive of the deployed source so its changelog expectations matched that
build during metadata finalization. Evidence is in
`test-results/browser/alpha2-production-chromium/` and
`test-results/browser/alpha2-production-firefox/`.

The final metadata commit is subject to CI, the browser matrix, deployment
verification, and normal/narrow version/startup checks before tagging. The
published GitHub release records its exact commit, deployment, CI, and final
smoke results alongside this candidate record.

## Feedback

Report reproducible problems at
<https://github.com/elschilling/nanquim/issues>. Include browser and
operating-system versions, reproduction steps, expected and actual results,
relevant console errors, and a small shareable SVG or DXF when possible. Remove
private drawing content before attaching files.
