# Nanquim v0.1 Public Beta Plan

| Field | Value |
| --- | --- |
| Status | Active |
| Owner | Project maintainer |
| Created | 2026-08-20 |
| Last reviewed | 2026-08-21 |
| Target | `v0.1.0-beta.1`, followed by promotion to `v0.1.0` |
| Baseline commit | `3d8221c` (Phase 0 implementation base) |
| Review cadence | At each milestone boundary and beta candidate |
| Source of truth | This document for pre-1.0 execution order |

## Purpose

Nanquim already has a broad pre-1.0 feature set. The next goal is to make a
complete technical-drawing workflow trustworthy, testable, teachable, and
releasable before expanding the command catalog.

The target sequence is:

```text
Repository truth
      -> Safe documents
      -> Predictable editing
      -> Qualified Paper Space and file exchange
      -> Onboarding and tutorials
      -> Public beta
      -> Feedback-driven features
```

This document is the active implementation roadmap. Superseded roadmap drafts
were consolidated here and removed from the working tree because they described
several delivered features as missing. Their historical context remains
available in Git history.

## Planning assumptions

- Reliability has priority over adding commands.
- Estimates assume one developer working on one primary engineering track at a
  time. Part-time work may take two to three times longer.
- Milestone dates are not commitments. Each phase ends when its acceptance
  criteria pass.
- Application versions and saved-document schema versions are independent.
- Persistent serialized Undo history is not beta scope; New/Open should reset
  history safely.
- Refactoring follows characterization tests; large modules are not rewritten
  speculatively.
- Every user-visible capability should eventually have tests, Help metadata,
  written documentation, and a content-impact decision.

## Current baseline

As of the review date:

- 30 terminal commands are registered in `src/js/commands/_commands.js`.
- Searchable F1 Help covers every command and includes one inline SVG diagram
  per command.
- An original theme-tintable icon set and local appearance preferences provide
  three desaturated presets plus custom accent/background colors.
- The full Vitest suite and V8 coverage ratchet pass. The current measured
  baseline and thresholds are maintained in `docs/testing.md` rather than
  duplicated in this roadmap.
- CI runs a frozen install, coverage, production build, high-severity
  dependency audit, and a production-build Chromium workflow. A separate
  scheduled/prerelease workflow defines the wider browser matrix.
- Native saves emit document schema v3. Historical schema-v1 and schema-v2
  fixtures migrate through the current loader, while Geometry Nodes retains
  its independent schema v1. A purpose-built schema-v3 fixture protects the
  complete semantic save/reopen boundary.
- Model Space, Paper Space, collections, blocks, SVG/DXF I/O, PDF output, and
  experimental Geometry Nodes are implemented.
- The published beta matrix makes desktop Chromium the primary target, Firefox
  the supported upload/download-fallback target, and Safari/WebKit best-effort
  with a manual prerelease check until automated coverage exists.

Important gaps remaining after the local Predictable Editing qualification:

- Changed legacy-coupled command/editor files remain under the global coverage
  ratchet and focused semantic tests. Add standalone-strength per-file floors
  as those boundaries are extracted and can be measured honestly.
- The remote browser matrix passes in current and previous-stable Chromium plus
  stable and ESR Firefox. Safari/WebKit remains a manual best-effort check.
- The direct persistent-file-handle overwrite/reopen workflow remains a manual
  Chromium candidate gate; the portable automated workflow deliberately uses
  isolated downloads and upload/reopen.
- `master` is now the documented canonical branch, GitHub's verified default,
  CI target, Vercel production branch, and local `origin/HEAD`. An active GitHub
  ruleset protects it, and the obsolete `svgcad` Vercel project has been
  removed.
- The package, build, annotated `v0.1.0-alpha.1` tag, changelog, and published
  GitHub prerelease agree on application version `0.1.0-alpha.1`.
- Mixed SVG.js runtimes, the large main chunk, and large coupled modules remain
  technical debt. The Phase 2 static/dynamic import overlap was removed.

## Milestone overview

| Phase | Milestone | Focus | Estimated focused effort |
| --- | --- | --- | ---: |
| 0 | Release baseline | Repository truth and governance | 2-4 days |
| 1 | Safe Documents | Persistence and document lifecycle | 3-5 weeks |
| 1.5 | Nanquim identity | Original icons, themes, and visual differentiation | Focused interlude |
| 2 | Predictable Editing | Commands, history, and browser tests | 4-6 weeks |
| 3 | Publish and Exchange | Paper Space and interoperability | 3-5 weeks |
| 4 | Learn and Scale | Onboarding, tutorials, and accessibility | 2-4 weeks |
| 5 | `v0.1.0-beta.1` | Release qualification and feedback | 2 weeks |
| Later | `v0.1.0` and beyond | Feedback-driven professional features | Ongoing |

## Phase 0 - Establish one source of truth

Status: Done.

Dependencies: None.

### Deliverables

- [x] Use `master` as the canonical branch and align the GitHub default branch,
  CI, branch rules, and local remote metadata. The active ruleset requires a
  pull request, the `Test, build, and audit` check, an up-to-date branch, linear
  history, squash merging, resolved review threads, and blocks deletion and
  force pushes.
- [x] Confirm that Vercel's production branch is `master` and retire any
  obsolete production project link.
- [x] Adopt the initial prerelease version `0.1.0-alpha.1`.
- [x] Derive the displayed application version from package/build metadata.
- [x] Keep the application version separate from `data-nanquim-version` and
  Geometry Nodes schema versions.
- [x] Add `CHANGELOG.md` and a documented release/deployment/rollback checklist.
- [x] Consolidate superseded roadmaps into this plan and remove their stale
  working copies while preserving history in Git.
- [x] Create one capability ledger that marks each area as stable,
  experimental, partial, or planned.
- [x] Refresh contributor architecture notes for Vite 6, Vitest, Paper Space,
  Blocks, sanitization, and Geometry Nodes.
- [x] Add purpose-built, licensed SVG/DXF samples under `tests/fixtures`.
- [x] Remove backup, generated, and test-only artifacts from `public`.
- [x] Add an asset provenance and license inventory.
- [x] Decide between bundled local fonts and remote fonts; do not ship both
  unintentionally.
- [x] Publish the exact beta browser/file-API support matrix and version policy.

### Acceptance criteria

- One documented branch controls CI and production.
- Application, package, tag, and release notes report the same version.
- README, capability ledger, and Help agree about supported functionality.
- Production contains no test-only fixtures or backup files.
- Every retained example and visual asset has known provenance.
- `pnpm test`, `pnpm build`, and `pnpm audit --audit-level high` pass.
- A maintainer can deploy and roll back from written instructions.

### Phase 0 evidence

Repository-side evidence as of 2026-08-20:

- `tests/application-version.test.js`, `tests/phase0-metadata.test.js`, and
  `tests/phase0-fixtures.test.js` protect version injection, independent schema
  domains, documentation/asset consistency, and semantic fixture baselines.
- `docs/capabilities.md`, `docs/browser-support.md`,
  `docs/asset-provenance.md`, `THIRD_PARTY_NOTICES.md`, and
  `docs/release-process.md` establish the new documentation sources of truth.
- The provenance inventory identifies every retained runtime visual/font by
  source project, embedded version where available, author/license, and local
  history. It explicitly records unresolved byte-level upstream revisions
  rather than claiming checksum-reproducible origins.
- The UI and Paper exporter share bundled local fonts; ordinary application use
  no longer requests Google Fonts.
- The full local suite passes 178 tests across 20 files, the production build
  succeeds, and `pnpm audit --audit-level high` reports no known
  vulnerabilities. At that checkpoint the build reported the documented
  mixed-import and main-chunk size warnings.
- Protected implementation PR
  [#12](https://github.com/elschilling/nanquim/pull/12), browser-qualification
  fix PR [#13](https://github.com/elschilling/nanquim/pull/13), and Paper PDF
  fix PR [#14](https://github.com/elschilling/nanquim/pull/14) passed the required
  CI check and were squash-merged. Exact-SHA post-merge CI run
  [32396829477](https://github.com/elschilling/nanquim/actions/runs/32396829477)
  passed for functional candidate `3876106df38313ade821987570a79120cb1ff042`.
- Release-finalization PR
  [#15](https://github.com/elschilling/nanquim/pull/15) passed the required CI
  check and was squash-merged as `0715d22ad99e41a89a7ccca93588b5a04a59656c`.
  Exact-SHA post-merge CI run
  [32398150915](https://github.com/elschilling/nanquim/actions/runs/32398150915)
  and Vercel Production deployment `6007398348` both succeeded. Fresh Chromium
  and Firefox startup/version checks passed against the public production alias
  without console, page, request, or HTTP errors.
- Production workflows pass on Fedora 44 in Chromium 151 and Firefox 153. The
  recorded checks cover startup/version/fonts, normal and narrow layouts, Help,
  line and dimensional-rectangle creation, selection, Undo/Redo, native SVG
  download/reopen and collection ownership, allowed/denied clipboard behavior,
  real Firefox SVG/DXF chooser imports, and visible Paper viewport/SVG/PDF
  output. The release notes record exact versions, scope, and automation limits.
- GitHub ruleset `Protect default branch` (ID `21099359`) is active on `master`.
  GitHub reports the required pull-request, squash-only linear history,
  resolved-thread, strict `Test, build, and audit`, deletion, and
  non-fast-forward protections as effective.
- Vercel's `nanquim` Production environment tracks `master`; Production
  deployment `6007180406` succeeded for exact commit
  `3876106df38313ade821987570a79120cb1ff042` at the protected immutable URL
  `nanquim-nsj3waj0z-elschillings-projects.vercel.app`. Final release deployment
  `6007398348` then succeeded for tag commit
  `0715d22ad99e41a89a7ccca93588b5a04a59656c`, and the public
  `nanquim.vercel.app` alias serves it. The obsolete `svgcad` Vercel project has
  been removed.
  `nanquim.is-a.dev` remains attached to Production while its external domain
  registration is pending; it is not a release endpoint until DNS verification
  succeeds.
- The annotated tag `v0.1.0-alpha.1` resolves to the final release commit, and
  the [GitHub prerelease](https://github.com/elschilling/nanquim/releases/tag/v0.1.0-alpha.1)
  is published. Its generated 3,864,016-byte source ZIP downloaded successfully
  and passed archive integrity validation.

## Phase 1 - Safe documents

Status: Done.

Dependencies: Phase 0 version and repository decisions.

### 1. Characterize the current document format

Before refactoring, create fixtures and failing tests for:

- Model collections and nested groups.
- Specialized arc, circle-trim, spline, hatch, and dimension metadata.
- Text and dimension styles.
- Blocks, definitions, instances, and internal references.
- Imported gradients, patterns, masks, clip paths, markers, symbols, and styles.
- Paper settings, viewports, and editable Paper annotations.
- Geometry Nodes graphs, instances, source ownership, and cached output.
- Native document schema v1 and v2 fixtures, plus Geometry Nodes schema v1.
- Text, collection/style names, block names, and metadata containing XML-special
  characters and both quote styles.

### 2. Introduce a canonical document serializer

Create one serialization boundary responsible for the entire editable document.
Saving from the navbar, direct file handles, downloads, tests, and future crash
recovery must use the same serializer.

The serializer must:

- Preserve semantic SVG rather than merely visible pixels.
- Preserve valid IDs and internal references without collisions.
- Include imported definitions and Paper annotations.
- Serialize collection, style, block, viewport, and Geometry Nodes state.
- Avoid interaction-only classes, helpers, handles, and transient selections.
- Escape text and attribute contexts correctly. Prefer structured XML/DOM
  serialization over ad hoc string replacement for names and metadata.
- Produce deterministic enough output for semantic comparisons.

### 3. Validate and migrate schema versions

- Parse and validate the saved document version.
- Add deterministic v1-to-current and v2-to-current fixtures and keep Geometry
  Nodes schema migration independent.
- Bound JSON size, depth, node count, and numeric ranges.
- Isolate invalid metadata while preserving safe geometry where possible.
- Decide whether a future-schema document is rejected before Open or presented
  through an explicit read-only mode. In either case it must not mutate the
  current document, be silently downgraded, or become writable as the current
  schema.
- Surface clear messages for migrated, partially recovered, and unsupported
  documents.

### 4. Add a complete document lifecycle

- Implement explicit New, Open, Save, and Save As behavior.
- Stage parsing, migration, sanitization, hydration, and manager loading before
  atomically replacing the live session. Any failure must preserve the previous
  document, dirty state, history, and file handle.
- Centralize reset of selection, commands, helpers, managers, collections,
  spatial indexes, file handles, and history.
- Track dirty/saved state from document mutations.
- Define native Save/Save As as lossless editable-document operations. Move
  white-to-black conversion and other presentation changes into explicit Export
  actions; exports do not clear dirty state or replace the native file handle.
- Warn before losing unsaved work through New, Open, reload, or tab close.
- Ensure fallback upload/download behavior cannot overwrite the wrong file.
- Keep persistent recent-file handles optional and permission-aware.

### Acceptance criteria

- A canonical document containing every supported subsystem survives
  `create -> save -> reopen -> save` without semantic loss.
- Paper annotations and imported definitions persist and render correctly.
- New/Open always starts with empty history and neutral interaction state.
- Undo from one document can never mutate another document.
- Corrupt metadata produces a bounded warning rather than a crash or unsafe
  document rewrite.
- A future-schema fixture follows the chosen reject/read-only policy without
  changing or downgrading the active document.
- A failure at any Open stage leaves the prior document, history, dirty state,
  and file handle intact.
- Save marks the session clean only after a successful disk write. Cancellation
  and write failure preserve dirty state and the previous handle.
- Export transformations do not change the editable document or native handle.
- XML-special characters in names, text, and metadata survive safely and
  exactly through the semantic round trip.
- Direct-handle and download/upload workflows have equivalent saved content.

### Phase 1 evidence

Repository-side evidence as of 2026-08-20:

- `DocumentSerializer`, `DocumentParser`, `DocumentState`, `DocumentController`,
  and the staged `DXFLoader` now form one bounded native-document boundary.
  Schema-v1/v2 inputs migrate to schema v3 and start dirty; future schemas are
  rejected before live state or file association changes.
- The schema-v3 fixture covers nested collections, specialized geometry,
  styles, definitions and local references, opaque block names, Paper
  annotations/viewports, root metadata, and Geometry Nodes graphs, instances,
  cached output, and persisted graph view state.
- Lifecycle tests inject parse, sanitization, manager, Paper-adoption, listener,
  permission, cancellation, write, and concurrent-operation failures. They
  verify exact rollback identities, neutral New/Open state, empty prior
  history, both dirty indexes, correct handles, and stale-operation rejection.
- Save and Save As share one side-effect-free serializer. A writable handle
  marks only the exact written revision clean after `close()` succeeds.
  Download-only Save As produces the same bytes but remains explicitly
  unverified and dirty because browsers expose no download-completion signal.
- The full suite passes 306 tests across 31 files. The production build and
  `pnpm audit --audit-level high` pass; at that checkpoint the pre-existing
  mixed-import and main-chunk size warnings remained.
- A clean-profile Chromium 151.0.7922.137 smoke check on Fedora 44 exercised the
  built app at 1280x800 and 960x720. It covered New, dirty tracking, Paper-mode
  native serialization, intercepted download-only output, an in-page
  writable-handle close contract, real file-input schema-v3 Open,
  Paper/definitions/Geometry Nodes restoration, future-schema rejection without
  mutation, and persisted viewBox dirtying,
  with no console, page, request, HTTP, or layout errors. At that checkpoint a
  persistent cross-browser CI harness remained Phase 2 work; Phase 2 has now
  delivered it.

## Phase 1.5 - Establish Nanquim's visual identity

Status: Done.

Dependencies: Stable preferences and the Phase 1 document boundary.

This focused interlude responds to community feedback while keeping the
productive editor-oriented workspace structure. Blender and Pablo Vazquez's
web mockups remain acknowledged workflow influences, but Nanquim now uses its
own expressive artwork and color relationships rooted in technical ink and
architectural drafting.

Delivered:

- Replaced the inherited Blender icon sheet and extracted derivatives with a
  project-authored, theme-tintable SVG mask sheet and original pen-nib mark.
- Added Oxide Red, Verdigris Green, and Blueprint Blue presets plus validated
  custom accent and background colors in Preferences.
- Kept appearance local to the browser: previews roll back on Cancel/Escape,
  saved choices persist locally, and themes never dirty drawing documents.
- Added adaptive light/dark foreground selection for arbitrary custom
  backgrounds, visible focus, reduced-motion, and forced-color accommodations.
- Restyled the existing frame, canvas, terminal, Outliner, Properties, dialogs,
  Help, welcome screen, Paper UI, and Geometry Nodes through semantic tokens
  without restructuring the workspace.
- Deepened the application rail and drafting field toward near-black and added
  roomier 24px/28px icon hosts while preserving 20px glyph artwork.
- Added hover descriptions and native keyboard/screen-reader semantics to the
  center drawing-aid controls, with synchronized pressed and disclosure state.
- Added a registry-driven left command palette with distinct original icons for
  all 30 commands, compact and categorized labeled modes, accessible resizing,
  persisted local layout state, and an `F4` visibility shortcut.
- Documented the identity and icon grammar in `docs/visual-identity.md` and
  removed the retired Blender icon notice from the distributed third-party
  inventory.

Evidence as of 2026-08-20:

- `tests/theme-preferences.test.js`, `tests/theme-styles.test.js`,
  `tests/icon-system.test.js`, and `tests/toolbar-accessibility.test.js` protect
  preference behavior, semantic styling, live icon coverage, accessible
  drawing-aid controls, and the absence of retired artwork references.
- `tests/command-icons.test.js` and `tests/tool-palette.test.js` additionally
  protect registry/icon parity, real layout wiring, local persistence,
  accessibility, resize cleanup, shortcut guards, and shared command dispatch.
- The full suite passes 336 tests across 37 files; the production build and
  `git diff --check` pass. At that checkpoint the pre-existing mixed-import and
  main-chunk warnings remained.
- Chromium 151 on Fedora 44 passes 28/28 appearance checks across Oxide Red,
  Verdigris Green, Blueprint Blue, and a persisted custom light palette. The
  run covers live preview and rollback, dirty-state isolation, the new icon
  mask, and 1280x800/960x720 layouts without console, page, request, HTTP, or
  horizontal-overflow errors.

## Phase 2 - Predictable editing

Status: In progress — the command, History, coverage baseline, browser harness,
and local and remote release-browser qualification are complete;
legacy-coupled coverage ratcheting and candidate manual gates remain.

Dependencies: Stable document reset and representative fixtures from Phase 1.

### 1. Add coverage and a deterministic test harness

- [x] Add Vitest coverage for modules under `src/js` while excluding vendored
  libraries; protect `src/main.js` startup through the production browser
  harness.
- [x] Record the initial baseline and ratchet thresholds upward.
- [x] Give new standalone Phase 2 modules a higher local threshold and document
  the separate policy for changed legacy-coupled modules.
- [x] Provide shared Editor, SVG, signal, terminal, collection, clipboard, timer,
  animation-frame, and file API fixtures.
- [x] Detect leaked listeners, subscriptions, timers, helpers, and interaction
  flags.

Remaining coverage debt: add equally strong per-file floors to legacy-coupled
modules as focused extraction and tests make those boundaries measurable.
Until then, the global ratchet and focused semantic regressions protect those
files; this roadmap does not claim that they meet the standalone-module floor.

### 2. Define a lifecycle contract for all registered commands

Every registered command now has a shared baseline covering:

- Canonical name, aliases, category, and Help metadata.
- Model Space versus Paper Space availability.
- Centralized invocation plus neutral cleanup after repeated cancellation.

Focused suites add the following command-specific checks where applicable;
their presence here does not claim that every command exposes every input or
mutation path:

- Pointer and typed input paths.
- Result geometry and specialized metadata.
- Active collection and inherited style.
- Preview/helper visibility and cleanup.
- Terminal prompts and recording of typed values.
- Escape, invalid input, and repeated cancellation.
- Selection, UI signals, and spatial-index invalidation.
- Undo and Redo against normalized document state.

Focused test work followed, and remaining expansion continues, in this risk
order:

1. Trim, Extend, Fillet, Offset, and their intersection helpers.
2. Move, Copy, Rotate, Scale, Mirror, and transformed geometry.
3. Snaps on arcs, ellipse arcs, splines, nested groups, and block instances.
4. Drawing and annotation commands.
5. Blocks, viewports, and Geometry Nodes mutations.

### 3. Make mutations transactional

- [x] Route new and changed mutations through one History execution path.
- [x] Stop adding new direct writes to `history.undos` or `history.redos`.
- [x] Migrate the user-facing command mutations in this milestone behind
  regression coverage.
- [x] Clear the redo branch after a new edit.
- [x] Define transaction boundaries for multi-step commands.
- [x] Document history behavior for Properties, collections, Paper Space, Blocks,
  and Geometry Nodes.

### 4. Centralize command invocation and cancellation

- [x] Resolve command names and aliases through one runner.
- [x] Give command sessions an explicit cleanup/disposal contract.
- [x] Return the editor to a neutral state after completion, error, or Escape.
- [x] Prevent orphan previews and duplicated signal subscriptions.
- [x] Keep terminal, toolbar, keyboard, and future Help actions behaviorally
  equivalent.

### 5. Add real-browser workflow tests

Use the existing open-source `puppeteer-core` harness with locally installed
stock browsers; ordinary development must not require a bundled or paid cloud
browser service.

Run current Chromium workflows on each pull request and `master` push. Run
current/previous Chromium plus Firefox stable/ESR in the separate scheduled,
manual, and prerelease workflow. Run a documented Safari/WebKit manual smoke
check before beta candidates while it remains best-effort. Preserve action,
console, network, screenshot, and available DevTools traces on automated
failure.

Minimum browser flows:

- [x] Create a rectangle using typed dimensions.
- [x] Select, Move, Undo, and Redo.
- [x] Cancel an active command without leaving helpers.
- [x] Save and reopen a representative SVG.
- [x] Copy and paste sanitized geometry.
- [x] Create and export a Paper viewport.
- [x] Open, search, and keyboard-navigate Help.
- [x] Load and evaluate a Geometry Nodes example.

### Acceptance criteria

- [x] Every registered command has at least one lifecycle contract.
- [x] High-risk geometry combinations have explicit supported/unsupported
  results.
- [x] Repeated start/cancel cycles leave stable listener and helper counts.
- [x] Undo/Redo round trips reproduce normalized document state.
- [x] Core workflows pass in every release-gate browser version named in the
  published beta support matrix without uncaught exceptions. The remote matrix
  qualifies stable and previous-stable Chromium plus stable and ESR Firefox.
- [ ] Record the direct Chromium persistent-handle workflow, best-effort
  Safari/WebKit, and remaining manual candidate checks
  from the [browser qualification checklist](../docs/browser-support.md#release-qualification-record)
  and [release process](../docs/release-process.md).
- [x] CI stores actionable browser artifacts for failures.

### Phase 2 evidence

Repository and local evidence as of 2026-08-21:

- Vitest's V8 provider measures modules under `src/js` with a reviewed global
  ratchet and higher focused thresholds for the new standalone Phase 2
  modules. Changed legacy-coupled files remain under the global ratchet and
  focused semantic regressions until extraction makes honest per-file floors
  practical. Production-browser startup protects `src/main.js` at the built-app
  boundary. `tests/support/deterministic-harness.js` supplies the shared SVG,
  Editor, signal, listener, timer, clipboard, and file-API fixtures. Exact
  current measurements live in `docs/testing.md`.
- All 30 registered commands share one runner and declare canonical metadata,
  aliases, Model/Paper availability, invocation, and repeated-cancellation
  behavior. Terminal, palette, keyboard repeat, Help, and cancellation consume
  that registry policy.
- History changes its stacks and dirty revision only after a successful
  Execute, Undo, or Redo. The covered draw, transform, grouping, block, hatch,
  paste, delete, trim replacement, text, dimension, viewport, and composite
  grip mutations restore identity, parent/order, selection, semantic metadata,
  and both spatial indexes where applicable. Paper viewport Redo rehydrates
  the same semantic id/state into a fresh live viewport with interaction wiring;
  failed removal and staged adoption preserve the exact prior live identities.
- Grip editing converts active-root pointer coordinates into each element's
  local coordinate system, including nested transforms, and applies Ortho in
  visible root space. Snaps qualify transformed line, arc, ellipse-arc,
  spline, intersection, and block-instance targets without traversing Block
  shadow content. ROTATE/SCALE reject transformed primitives and transformed
  ancestors while preserving composition for selected groups/Block instances
  whose ancestors are untransformed; MIRROR rejects own or inherited
  transforms. Unsafe transformed TRIM, EXTEND, and FILLET cases stop with an
  explicit diagnostic before mutation. HATCH rejects transformed scopes that
  may affect the selected region but permits provably remote transformed
  geometry. OFFSET similarly qualifies only untransformed lines, circles, and
  square-corner rectangles and rejects its unsupported inputs before ghosting
  or mutation.
- BLOCK requires a same-parent selection and proves selected nodes, ancestors,
  and descendant trees untransformed before opening its commit path. Definition
  children follow source order, the instance occupies the earliest selected
  slot, and failure/Undo restore exact source positions and selection order.
- Model selection excludes hidden/locked Model content and all Paper
  annotations; Paper selection excludes Model geometry and honors viewport and
  annotation visibility/locking. Visible locked Model geometry remains in
  Model boundary discovery while hidden content does not.
- Delete collapses nested selections into canonical roots and restores exact
  nodes, parents, sibling order, and prior selection. Paper viewport deletion
  is rejected explicitly until it has an equivalent History transaction.
  Paste sanitizes and scopes content before adoption, assigns stable IDs,
  repairs local references, and reuses the same nodes and IDs through
  Undo/Redo. Session revision guards prevent guarded delayed command callbacks
  from affecting a successor, and a delayed Clipboard API read is discarded if
  the command session or captured document state changes.
- On Fedora 44, the production harness passes all nine workflows in Chromium
  151.0.7922.137 and Firefox 153.0.3. It covers startup, actual pointer
  selection, typed rectangle creation, Move/Undo/Redo, repeated cancellation,
  sanitized clipboard round-trip, and canonical native save/reopen/resave with
  exact bytes, geometry, ownership, dirty state, History, and fallback-handle
  assertions. The same run covers real Paper annotation pointer hover, click,
  disambiguation, and Move/Undo/Redo with parsed viewport SVG output, Help
  keyboard navigation, and Geometry Nodes evaluation.
- CI definitions now run current Chromium on pull requests and `master` after
  the required test/build/audit job, and define the scheduled/prerelease
  current/previous Chromium and Firefox stable/ESR matrix. A manually
  dispatched remote run passes all four matrix targets. Direct
  persistent-handle overwrite/reopen, Safari/WebKit, and the remaining checks
  in the
  [browser qualification checklist](../docs/browser-support.md#release-qualification-record)
  and [release process](../docs/release-process.md) are still required before
  this phase can be marked Done.

## Phase 3 - Qualify Paper Space and file exchange

Status: Not started.

Dependencies: Canonical serializer, command contracts, and browser harness.

### 1. Finish production Paper Space

- Persist annotations and all viewport state, including locking and visibility.
- Make viewport locking functional and durable.
- Verify scale presets, model origins, and ISO A0-A4 dimensions.
- Preview color mappings accurately and preserve ordinary SVG stroke widths.
- Make an explicit scope decision before adding per-color plot-lineweight
  mapping; it is not assumed to be an existing beta feature.
- Verify local font embedding and dimension readability.
- Preserve vector output in standalone SVG and PDF.
- Decide whether multi-sheet documents are pre-1.0 scope. The default proposal
  is one reliable sheet for the first beta.

### 2. Build an interoperability qualification suite

Curate small fixtures for primitives, arcs, ellipses, polylines, splines, text,
collections, blocks, transforms, hatches, Paper Space, and unsupported entities.

Check semantic results against:

- Browsers and Inkscape for SVG.
- A pinned LibreCAD version for DXF. QCAD Community may be an additional manual
  target only when its exact version is recorded.
- A pinned Blender version for the first SVG interoperability profile.

Compare entity counts, bounds, units, collections/layers, styles, and internal
references within documented tolerances. Report skipped or degraded entities to
the user rather than silently dropping them.

### 3. Establish performance budgets

Create fixed 1,000- and 10,000-element drawings and measure:

- Load and save.
- Spatial-index rebuild and queries.
- Pan, zoom, window selection, and snapping.
- Outliner rendering and synchronization.
- Geometry Nodes evaluation.

Record median and upper-percentile timings with tolerant regression budgets.
Optimize only after a behavior and performance baseline exists.

### Acceptance criteria

- Paper fixtures export at correct physical sizes and viewport scales.
- Color, existing stroke-width, font, dimension, and vector-output checks pass.
- Supported SVG/DXF entities preserve documented semantics and tolerances.
- Unsupported entities generate an understandable summary.
- Test fixtures are not present in production output.
- Performance regressions are measurable before optimization begins.

## Phase 4 - Onboarding, documentation, and tutorials

Status: Not started.

Dependencies: A tagged, stable workflow from Phases 1-3. Documentation work can
begin earlier, but recordings should target a tagged application version.

### 1. Build task-based documentation

Proposed structure:

```text
docs/
  getting-started.md
  tutorials/first-drawing.md
  guides/terminal-and-coordinates.md
  guides/snaps-and-tracking.md
  guides/collections-and-properties.md
  guides/paper-space.md
  guides/geometry-nodes.md
  reference/commands.md
  reference/file-compatibility.md
  troubleshooting.md
```

- Keep README as the landing page.
- Generate or validate command reference data against the command registry.
- Extend command metadata with syntax, ordered steps, options, examples, and
  supported editor contexts.
- Reuse the Help illustration grammar in written guides and video callouts.
- Mark Geometry Nodes and other unstable features as experimental.
- Add tested-version information to task tutorials.

### 2. Improve first-run onboarding

- Add Open Example and First Drawing Guide actions to the Welcome screen.
- Provide small, licensed starter and completed drawings.
- Add a dismissible checklist for pan/zoom, Rectangle dimensions, snaps, Undo,
  Save, and F1 Help.
- Make the application version and experimental status visible.
- Keep onboarding keyboard accessible and independent of persistent file APIs.

### 3. Create a repeatable Fedora/FOSS content workflow

- OBS Studio: capture to MKV, then remux for editing.
- Blender Video Sequence Editor: editing, crops, transitions, and text strips.
- Inkscape: SVG thumbnails and title cards.
- FFmpeg: inspection and final encoding.
- Captions: concise burned-in instructional text plus an equivalent `.srt`.

Use 1920x1080 at 30 fps unless testing demonstrates a better target. Verify
terminal text, cursor, snap markers, helpers, and dimensions at YouTube's 720p
playback size. Keep rendered video and large `.blend` files outside the
application repository.

### 4. Publish a starter batch

Publish three caption-first videos before broadly announcing the channel:

1. **Nanquim: Browser-Based SVG CAD in Two Minutes**
2. **Your First Precise Drawing**
3. **Terminal, Coordinates, Ortho, and Snaps**

Each episode requires a storyboard, versioned sample, captions, thumbnail,
chapters, description, tested application version, limitations, and links to
the demo, source, written guide, and issue tracker.

Continue with selection/editing, curves, collections, annotation, Blocks,
interoperability, Paper Space, and Geometry Nodes at a sustainable cadence of
one four-to-eight-minute tutorial every one or two weeks. Keep two completed
episodes in reserve when possible.

### Acceptance criteria

- In a clean browser profile, at least three first-time participants can follow
  only the published guide to create, dimension, save, close, and reopen the
  sample; all complete it and the median completion time is under ten minutes.
- Help and the written command reference cannot drift silently.
- Onboarding is dismissible, restartable, keyboard accessible, and works with
  browser file fallbacks.
- Every episode can be reproduced from its tagged sample and written guide.
- Captions remain complete and readable when watched muted at 720p.
- User-facing changes receive a docs/Help/video impact decision.

## Phase 5 - Public beta and feedback

Status: Not started.

Dependencies: Exit criteria from Phases 0-4 for the supported beta surface.

### Release deliverables

- Tag and publish `v0.1.0-beta.1` with a changelog, compatibility notes, and
  known limitations. Promote to `v0.1.0` only after at least a 14-day beta soak,
  no unwaived P0/P1 issue in the supported surface, and a fresh full release
  qualification run.
- Document deployment verification and rollback.
- Add bug, file-compatibility, and feature-request issue templates.
- Add a pull-request checklist for tests, documentation, screenshots, saved
  schema impact, and release-note classification.
- Add a security reporting policy.
- Run a short feature freeze while beta feedback is triaged.

### Severity model

- **P0:** Data loss, document corruption, or security vulnerability.
- **P1:** A broken core drawing, editing, save/reopen, or export workflow.
- **P2:** Serious interoperability, browser, performance, or usability issue.
- **P3:** Enhancement or new capability.

### Beta release gate

- No unwaived P0 or P1 issues in the supported beta surface. A waiver must name
  the owner, user impact, workaround, and target fix release.
- Canonical round-trip fixtures pass.
- All registered commands have lifecycle coverage.
- Supported browser smoke tests pass.
- Paper output and interoperability fixtures meet documented tolerances.
- Compatibility limitations and backup guidance are published.
- Deployment and rollback have been exercised.
- The beginner guide and starter tutorial batch are available.

## Post-beta feature order

Choose additions from observed workflows rather than the old feature list.
Suggested batches are:

1. Explode, Join, and Break.
2. Chamfer.
3. Ordinary rectangular and polar Array commands.
4. Construction lines and rays.
5. Angular, radius, and diameter dimensions.
6. Leaders and callouts.
7. Multiple sheets and reusable title-block templates.
8. Advanced accessibility, browser parity, and localization improvements beyond
   the baseline keyboard, focus, and dialog requirements for beta.

Every new command must satisfy the cross-cutting definition of done below.

## Blender and Bonsai path

Treat interoperability as an incremental program:

1. Document reliable SVG/DXF unit, origin, layer, and curve workflows.
2. Add a Nanquim export profile qualified against Blender import.
3. Publish versioned sample drawings and a Blender workflow tutorial.
4. Prototype a small importer or add-on only after the exchange profile is
   reliable.
5. Consider direct Bonsai connectivity after the Nanquim document and entity
   schemas are stable.

Parametric constraints, real-time collaboration, cloud storage, a plugin
architecture, and touch-first UI remain research topics until the core document
model reaches stability.

Persistent Undo/Redo serialization is also deferred. Pre-1.0 document changes
should instead make New/Open reset history reliably.

## Cross-cutting engineering work

These tasks support the milestones but should not displace user-facing safety
work:

- Consolidate browser-global SVG.js and npm SVG.js into one pinned runtime after
  command and browser tests protect current behavior.
- Stop adding new dependencies on undeclared globals.
- Sass imports, global functions, retained public-asset URLs, and Phase 2's
  mixed static/dynamic imports are migrated; next resolve the main bundle-size
  warning.
- The UI and Paper export now share bundled local fonts; add bundle-size
  reporting and budgets.
- Add ESLint, a formatting check, and incremental `checkJs`/JSDoc validation
  without a repository-wide formatting rewrite.
- Continue reference-audited dead-code cleanup as new evidence appears; do not
  remove code speculatively.
- Extract pure geometry and lifecycle services from large modules incrementally
  behind tests. Do not begin with a wholesale rewrite.

## Definition of done for user-facing changes

A feature or command is complete only when the relevant items are satisfied:

- Registry name, aliases, category, description, and context are correct.
- Help content and illustration are present or explicitly unaffected.
- Pointer and typed input paths behave consistently.
- New geometry uses the active destination and expected inherited style.
- Helpers are visible, non-persistent, and removed on completion/cancellation.
- Terminal prompts and typed values are recorded clearly.
- Undo/Redo and redo invalidation are correct.
- Selection, Outliner, Properties, and spatial indexes update correctly.
- Save/reopen preserves the feature's semantic state.
- SVG, DXF, and Paper export effects are tested or documented as unsupported.
- Unit/integration tests pass; browser-visible changes receive a real-browser
  check.
- Accessibility, documentation, sample, and tutorial impact are reviewed.
- No unrelated production assets, generated output, or build warnings are
  introduced.

## Immediate implementation queue

The first focused work package should be small enough to review independently:

1. **Done:** qualify and publish `v0.1.0-alpha.1`. The protected `master`
   branch, CI, Vercel Production deployment, immutable tag, release notes, and
   GitHub prerelease are verified.
2. **In progress:** the capability ledger exists; convert its remaining partial
   areas into a labeled issue backlog.
3. **Done:** small, purpose-built, licensed SVG/DXF fixtures have semantic
   parser and sanitizer assertions.
4. **Done:** Paper annotations and imported definitions have semantic
   round-trip regressions.
5. **Done:** Open/New reset history and interaction state, with failure and
   exact-rollback tests.
6. **Done:** schema v3 is written through one canonical serializer boundary.
7. **Done:** document dirty state, New, Save, and Save As have race-safe
   lifecycle contracts.
8. **Done:** add the coverage provider, ratchet, and shared deterministic
   command harness.
9. **Done:** implement lifecycle, support-policy, transaction, and cleanup
   contracts for Trim, Extend, Fillet, Offset, and the other registered
   commands.
10. **In progress:** the maintained production browser harness and CI jobs are
    implemented, all nine workflows pass locally in Chromium 151.0.7922.137 and
    Firefox 153.0.3, and the remote stable/previous-Chromium plus
    stable/Firefox-ESR matrix passes. Record the candidate manual gates before
    closing Phase 2.
11. Write the first-drawing guide against a tagged fixture.
12. Produce a 45-60 second caption-first recording pilot before the full video
    batch.

## Roadmap maintenance

- Review this file at every milestone boundary and before recording a tutorial
  batch.
- Use `Not started`, `In progress`, `Blocked`, and `Done` for phase status.
- Update Last reviewed whenever status or scope changes.
- Record roadmap-only scope changes in the decision log and pull-request
  rationale. Use the product changelog only when released behavior also changes.
- Mark completed work with issue, pull-request, automated-test, release, or
  recorded manual-check evidence rather than deleting its history immediately.
- Re-estimate only unfinished work.
- If code and this roadmap disagree, verify the application and tests, then
  update the roadmap in the same change when appropriate.

## Risk register

| Risk | Owner | Mitigation | Trigger for escalation |
| --- | --- | --- | --- |
| Historical v1/v2 or current v3 document loses semantic data | Persistence milestone owner | Versioned semantic fixtures and atomic migration tests | Any count, reference, style, Paper, Block, or Geometry Nodes mismatch |
| Open partially replaces the active session | Persistence milestone owner | Stage and validate before atomic replacement | Any failure changes drawing, dirty state, history, or file handle |
| Browser file APIs differ or permissions disappear | Browser/lifecycle owner | Capability checks, explicit fallbacks, and supported-browser tests | Wrong-file overwrite, lost save, or unrecoverable permission failure |
| SVG/DXF exchange silently degrades content | Interoperability owner | Pinned tools, fixture expectations, and user-visible summaries | Unsupported entity disappears without a warning |
| Example or visual asset has unclear reuse rights | Documentation owner | Provenance inventory and licensed fixtures | Missing author/source/license evidence |
| Beta scope expands faster than reliability work | Project maintainer | Phase gates, P0/P1 freeze, and decision log | New feature delays an unmet safety gate |

## Decision log

| Date | Decision | Reason |
| --- | --- | --- |
| 2026-08-20 | Make reliability the next milestone instead of adding commands | The existing feature surface is broad, while persistence and full workflow coverage remain the main user risks. |
| 2026-08-20 | Target `v0.1.0-beta.1` before a stable `v0.1.0` | The project has no prior tags and should qualify document compatibility through a public beta. |
| 2026-08-20 | Defer persistent history, direct Blender/Bonsai integration, constraints, collaboration, plugins, cloud storage, and touch-first UI | These depend on a stable document and entity model. |
| 2026-08-20 | Remove unreferenced legacy samples instead of promoting them to fixtures | The files had no automated consumers or documented provenance; future compatibility fixtures should be small, licensed, and assertion-backed. |
| 2026-08-20 | Use and protect `master` as the canonical branch | GitHub's default branch, CI, local remote pointer, active ruleset, and Vercel Production environment align on `master`; the obsolete `svgcad` project was removed. |
| 2026-08-20 | Start application releases at `0.1.0-alpha.1` without changing document schemas | Application SemVer, native SVG schema v2, and Geometry Nodes schema v1 serve different compatibility domains. |
| 2026-08-20 | Use bundled local fonts for both UI and Paper output | One project-controlled runtime source works offline after assets load, avoids a third-party request, and makes the four bundled families available to both paths. Exact variant parity remains Paper-output qualification work. |
| 2026-08-20 | Gate desktop Chromium and Firefox fallback workflows; keep Safari/WebKit best-effort for beta | Direct File System Access is Chromium-specific, while upload/download fallbacks provide the portable workflow. Automated browser coverage did not exist when this policy was adopted. |
| 2026-08-20 | Accept source project, embedded version, author/license, and local history as the Phase 0 provenance record | Exact upstream revisions for the inherited font binaries and Blender icon sheet cannot be recovered from current history. The inventory exposes that boundary; future replacements must be checksum-pinned rather than silently asserting an origin. |
| 2026-08-20 | Publish `v0.1.0-alpha.1` and close Phase 0 | The protected release workflow, exact-SHA CI and Vercel deployments, Chromium/Firefox qualification, annotated tag, prerelease, and source archive all passed their recorded checks. |
| 2026-08-20 | Adopt native document schema v3 and close Phase 1 | Schema v3 canonically includes model definitions, Paper annotations/viewports, root metadata, and bounded Geometry Nodes state; v1/v2 migrate dirty and future schemas fail before mutation. |
| 2026-08-20 | Treat native Save and presentation Export as separate operations | Editable Save is lossless and may clean only after a verified writable-handle close; exports and unverified browser downloads never retarget or clean the session. |
| 2026-08-20 | Use staged transactional Open/New with session and revision guards | Detached preparation, exact structural rollback, centralized session reset, and stale-operation rejection prevent partial replacement and wrong-file writes. |
| 2026-08-20 | Establish a distinct Nanquim visual identity before Phase 2 | Community feedback supported retaining the editor-oriented workflow while recommending an independent identity. Original technical-ink icons and themeable drafting surfaces reduce confusion and give Nanquim a recognizable origin of its own. |
| 2026-08-21 | Use one failure-atomic History boundary and registry-wide command contract for Predictable Editing | A completed user mutation must advance History and dirty state once, while cancellation or failed apply restores exact geometry, identity, selection, and indexes. Shared invocation and mode metadata prevent terminal, palette, keyboard, and Help behavior from drifting. |
| 2026-08-21 | Use V8 coverage plus a stock-browser `puppeteer-core` workflow and keep Phase 2 open until release qualification is complete | The locally reproducible harness protects real production behavior without a cloud-only dependency. Current and previous Chromium plus stable and ESR Firefox pass the remote matrix; direct persistent handles and Safari/manual candidate gates still need recorded evidence. |
