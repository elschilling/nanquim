import { SGV } from '@svgdotjs/svg.js' // Assuming SVG.js

/**
 * Recursively applies all nested transforms (translation, rotation, scale, etc.)
 * directly into the geometry of the leaf SVG elements (paths, lines, polygons)
 * and removes the `transform` attributes from groups.
 */
export function bakeTransform(element) {
    // We will implement this
}
