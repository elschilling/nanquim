import { SVG } from '@svgdotjs/svg.js'
import { multiplicity } from '../libs/dxf/src/util/toPiecewiseBezier' // if needed

export function flattenTransformGroups(editor) {
    const root = editor.drawing
    // recursive walk
    const walk = (node, ctm) => {
        //...
    }
}
